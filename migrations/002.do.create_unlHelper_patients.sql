CREATE TABLE rooms (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, -- change to uuid for randomization
    number INTEGER NOT NULL UNIQUE,
    status TEXT NOT NULL DEFAULT 'clean',
    liftRoom BOOLEAN NOT NULL DEFAULT 'false',
    nurseId INTEGER REFERENCES nurses(id) ON DELETE CASCADE NOT NULL,
    techId INTEGER REFERENCES techs(id) ON DELETE CASCADE NOT NULL 
);